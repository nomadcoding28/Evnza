spring.application.name=booking-service
server.port=8085


# Database Configuration (Port 3307 for XAMPP)
spring.datasource.url=jdbc:mysql://localhost:3307/db_booking?useSSL=false
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
# --- Actuator Configuration (Fixes Eureka Link) ---
management.endpoints.web.exposure.include=health,info,metrics
management.info.env.enabled=true

# --- Info Page Details ---
info.app.name=${spring.application.name}
info.app.description=Main orchestration service for tickets
info.app.version=1.0.0
